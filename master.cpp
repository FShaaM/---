#include <iostream>

int** creat (size_t row, size_t col);

void remove (int** m, size_t row, size_t col);

int main {

size_t row = 0, row = 0;

std::cin >> row >> col;

if (!std::cin)
	return 1;



std::cout << row << " " << col; << '\n';


} 
